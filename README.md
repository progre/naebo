How to build
====

1. make secret.json
```
{
  "twitter": {
    "consumer_key": "???",
    "consumer_secret": "???",
    "access_token_key": "???",
    "access_token_secret": "???"
  }
}
```
2. $ git submodule init
3. $ git submodule update
4. $ npm install -g gulp
5. $ npm install
6. $ gulp
