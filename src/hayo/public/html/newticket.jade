.panel.panel-default.anim.anim188px(ng-show='isOpen')
  .panel-body
    form(role='form', ng-submit='command.execute()')
      ul.list-inline
        li {{user.displayName}} さんのネタ
        li.checkbox(style='margin-left: 1em; margin-top: 0;')
          label
            input(type='checkbox', ng-model='isPost')
            | 作成時にツイートする
      .form-group
        input.form-control(type='text', placeholder='アイディア', ng-model='title', required)
      .form-group.text-right を作って！
      .text-right
        button.btn.btn-default(type='button', ng-click='close()') 閉じる
        input.btn.btn-primary(style='margin-left: 1em;', type='submit', value='作成', ng-disabled='!command.canExecute()')
