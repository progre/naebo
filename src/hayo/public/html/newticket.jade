.panel.panel-default
  .panel-body
    form(role='form', ng-submit='')
      ul.list-inline
        li @progremaster さんのネタ
        li.checkbox(style='margin-left: 1em; margin-top: 0;')
          label
            input(type='checkbox')
            | 作成時にツイートする
      .form-group
        input.form-control(type='text', placeholder='アイディア')
      .form-group.text-right を作って！
      .text-right
        button.btn.btn-default(ng-click='') 閉じる
        input.btn.btn-primary(style='margin-left: 1em;', type='submit', value='作成')
